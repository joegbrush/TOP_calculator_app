function add(a, b) {
  let sum = a + b;
  console.log(sum);
  return sum;
}

function subtract(a, b) {
  let diff = a - b;
  console.log(diff);
  return diff;
}

function multiply(a, b) {
  let product = a * b;
  console.log(product);
  return product;
}

function divide(a, b) {
  let quotient = a / b;
  console.log(quotient);
  return quotient;
}

add(2, 2);
subtract(2, 2);
multiply(2, 2);
divide(2, 2);
